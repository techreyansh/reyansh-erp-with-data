# Used only when running `npm run build` (including Vercel). Not exposed to the browser.
# CRA treats ESLint warnings as errors when CI=true; this repo has legacy warnings — remove this
# after cleaning lint, or set CI=false in the host (not recommended).
DISABLE_ESLINT_PLUGIN=true
